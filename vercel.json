{
  "buildCommand": "npm install && npx ng build --configuration production",
  "outputDirectory": "dist/interactive-developer-portfolio",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
