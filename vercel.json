{
  "buildCommand": "ls -la && npx ng build --configuration production && ls -la dist",
  "outputDirectory": "dist/portfolio/browser",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
